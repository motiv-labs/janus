---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: quay.io/hellofresh/ci-ansible

run:
  path: api-gateway-master/ci/tasks/deploy.sh

inputs:
  - name: automation-release
  - name: gh-release-final
  - name: api-gateway-master
